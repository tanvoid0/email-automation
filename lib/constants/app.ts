/** Display name (title case, shown in UI and browser tab). */
export const APP_NAME = "Grad Buddy";

/** One line under the home hero title. */
export const APP_TAGLINE =
  "Grad school outreach, applications, and documents—organized with AI.";

/** Meta description (SEO / link previews). */
export const APP_DESCRIPTION =
  "Your companion for grad admissions: professor outreach, application tracking, SOPs, and materials—with AI where it helps.";

/** Kebab-case slug: package name, default DB name, Docker image basename, mock email host. */
export const APP_SLUG = "grad-buddy";

/** Domain-style host for dev/mock Message-IDs (`mock-…@grad-buddy.local`). */
export const APP_MOCK_EMAIL_DOMAIN = `${APP_SLUG}.local`;

/** Login card helper line. */
export const APP_LOGIN_INTRO = `Sign in to ${APP_NAME}`;
